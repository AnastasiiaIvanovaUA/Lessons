// Написати стрілочну функцію, яка повертатиме остачу від ділення 
// Дана функція приймає два параметри, другий з яких за замовчуванням дорівнює 2 
let remainingFromDividing = (a, b = 2) => a % b;
console.log(remainingFromDividing(а));